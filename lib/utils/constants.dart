class Constants {
  // For Android emulator use 10.0.2.2 to reach localhost on host machine.
  // If you run backend on another host or device, replace this with the machine's IP.
  static const String baseUrl = 'http://localhost';
  static const String tokenKey = 'auth_token';
}
